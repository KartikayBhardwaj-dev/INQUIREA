'use client'
import { Check, ExternalLink, LogOut, Mail, RefreshCw, ShieldCheck } from 'lucide-react'
import { Button, Card } from './ui'
export function SettingsView({onSync}:{onSync:()=>void}){return <div className="flex max-w-3xl flex-col gap-7"><div><p className="mb-2 text-xs font-semibold uppercase tracking-[.18em] text-[var(--mint)]">Workspace</p><h1 className="text-3xl font-semibold tracking-tight">Settings</h1><p className="mt-2 text-sm text-[var(--muted)]">Manage your connection and assistant preferences.</p></div><Card><div className="flex items-center justify-between border-b border-[var(--line)] p-5"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-lg bg-[#302d3f] text-[#c7b5eb]"><Mail size={19}/></span><div><h2 className="font-semibold">Gmail connection</h2><p className="text-sm text-[var(--muted)]">hello@inquirea.dev</p></div></div><span className="flex items-center gap-1.5 text-xs text-[var(--mint)]"><span className="size-2 rounded-full bg-[var(--mint-strong)]"/>Connected</span></div><div className="flex flex-col gap-4 p-5"><div className="flex items-center justify-between"><div><p className="text-sm">Last sync</p><p className="mt-1 text-xs text-[var(--muted)]">Today at 9:48 AM · 42 emails</p></div><Button variant="outline" onClick={onSync}><RefreshCw size={14}/>Sync now</Button></div><button className="flex items-center gap-2 text-sm text-[var(--muted)] hover:text-[var(--foreground)]">Reconnect Gmail <ExternalLink size={14}/></button></div></Card><Card><div className="border-b border-[var(--line)] p-5"><h2 className="font-semibold">Assistant preferences</h2><p className="mt-1 text-sm text-[var(--muted)]">How Inquirea should help you respond.</p></div><div className="flex flex-col gap-5 p-5"><label className="flex items-center justify-between gap-4"><span><span className="block text-sm">Include email citations</span><span className="mt-1 block text-xs text-[var(--muted)]">Show the source threads behind every answer.</span></span><input type="checkbox" defaultChecked className="size-4 accent-[var(--mint-strong)]"/></label><label className="flex items-center justify-between gap-4"><span><span className="block text-sm">Concise reply drafts</span><span className="mt-1 block text-xs text-[var(--muted)]">Keep generated replies short and direct.</span></span><input type="checkbox" defaultChecked className="size-4 accent-[var(--mint-strong)]"/></label></div></Card><Card className="p-5"><div className="flex items-start gap-3"><ShieldCheck size={18} className="mt-0.5 text-[var(--mint)]"/><div><h2 className="text-sm font-semibold">Private by design</h2><p className="mt-1 text-sm leading-6 text-[var(--muted)]">Your inbox data is only used to answer your requests. API mode is currently set to preview fallback until a backend URL is configured.</p></div></div></Card><Button variant="ghost" className="w-fit"><LogOut size={15}/>Sign out</Button></div>}
