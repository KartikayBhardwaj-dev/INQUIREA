'use client'

import { useState } from 'react'
import { BarChart3, Bot, ChevronDown, Inbox, LogOut, Menu, Settings, Sparkles, X } from 'lucide-react'
export type View = 'dashboard' | 'chat' | 'settings'

export function AppShell({ view, onViewChange, children }: { view: View; onViewChange: (view: View) => void; children: React.ReactNode }) {
  const [open, setOpen] = useState(false)
  const items = [{ id: 'dashboard' as View, label: 'Dashboard', icon: BarChart3 }, { id: 'chat' as View, label: 'Chat Bot', icon: Bot }, { id: 'settings' as View, label: 'Settings', icon: Settings }]
  return <div className="min-h-screen bg-[var(--background)]"><header className="flex items-center justify-between border-b border-[var(--line)] bg-[var(--surface)] px-4 py-3 lg:hidden"><button type="button" onClick={() => setOpen(true)} aria-label="Open navigation"><Menu className="size-5" /></button><button type="button" onClick={() => onViewChange('dashboard')} className="flex items-center gap-2"><span className="grid size-8 place-items-center rounded-lg bg-[var(--blue)] text-white"><Sparkles className="size-4" /></span><span className="text-sm font-bold tracking-wide">INQUIREA</span></button><span className="size-5" /></header><aside className={`fixed inset-y-0 left-0 z-20 flex w-64 flex-col border-r border-[var(--line)] bg-[var(--surface)] p-5 transition-transform lg:translate-x-0 ${open ? 'translate-x-0' : '-translate-x-full'}`}><div className="flex items-center justify-between"><button type="button" onClick={() => onViewChange('dashboard')} className="flex items-center gap-3 text-left"><span className="grid size-9 place-items-center rounded-xl bg-[var(--blue)] text-white"><Sparkles className="size-4" /></span><span><span className="block text-sm font-bold tracking-wide">INQUIREA</span><span className="block text-[10px] text-[var(--muted)]">AI EMAIL COPILOT</span></span></button><button type="button" className="lg:hidden" onClick={() => setOpen(false)} aria-label="Close navigation"><X className="size-4" /></button></div><div className="mt-10 flex flex-col gap-2">{items.map(({ id, label, icon: Icon }) => <button type="button" key={id} onClick={() => { onViewChange(id); setOpen(false) }} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm transition ${view === id ? 'bg-[var(--blue-soft)] font-semibold text-[var(--blue)]' : 'text-[var(--muted)] hover:bg-[var(--surface-2)] hover:text-[var(--foreground)]'}`}><Icon className="size-4" />{label}{id === 'chat' && <span className="ml-auto rounded-full bg-[var(--blue)] px-1.5 text-[10px] font-bold text-white">2</span>}</button>)}</div><div className="mt-auto flex flex-col gap-4"><div className="rounded-xl border border-[var(--line)] bg-[var(--surface-2)] p-3"><div className="mb-2 flex items-center justify-between text-[11px] text-[var(--muted)]"><span>Gmail account</span><span className="size-2 rounded-full bg-[#0f9d58]" /></div><div className="flex items-center gap-2"><span className="grid size-8 place-items-center rounded-full bg-[#ea4335] text-xs font-bold text-white">A</span><div className="min-w-0"><p className="truncate text-xs font-semibold">alex.morgan@gmail.com</p><p className="text-[11px] text-[var(--muted)]">Connected</p></div><ChevronDown className="ml-auto size-4 text-[var(--muted)]" /></div></div><div className="flex items-center justify-between border-t border-[var(--line)] pt-4"><div className="flex items-center gap-2"><span className="grid size-8 place-items-center rounded-full bg-[var(--blue-soft)] text-xs font-semibold text-[var(--blue)]">AM</span><div><p className="text-xs font-semibold">Alex Morgan</p><p className="text-[11px] text-[var(--muted)]">Free plan</p></div></div><LogOut className="size-4 text-[var(--muted)]" /></div></div></aside><div className="lg:pl-64">{children}</div></div>
}
